# from databases import Database
# import sqlalchemy

# # Replace './test.db' with the actual path to your SQLite database file
# DATABASE_URL = "sqlite:///../db/artbasetwo.db"

# # This database instance will be used for asynchronous database operations
# database = Database(DATABASE_URL)

# # For synchronous operations, you'll need to create an engine with SQLAlchemy
# engine = sqlalchemy.create_engine(DATABASE_URL, connect_args={"check_same_thread": False})
